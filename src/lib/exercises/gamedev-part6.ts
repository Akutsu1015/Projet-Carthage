import { addExercises } from "./registry";
import type { Exercise } from "./registry";

// Chapitre 6 — Jeu Complet (exercices 26-30)
const exercises: Exercise[] = [
    {
        id: "gd_c6_e1",
        title: "Système de score",
        category: "Jeu Complet",
        description: "Ajoute un score qui augmente quand tu détruis un ennemi !",
        instruction: `Déclare <code>score = 0</code>. Quand un projectile touche un ennemi, incrémente le score de 100. Affiche le score en haut à droite avec <code>ctx.fillText</code>.`,
        code_template: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px = 50, py = 180, score = 0;\nconst bullets = [], enemies = [];\nlet frame = 0;\nconst keys = {};\nwindow.addEventListener("keydown", (e) => { keys[e.key]=true; if(e.key===" ") bullets.push({x:px+30,y:py+12,w:12,h:4}); });\nwindow.addEventListener("keyup", (e) => { keys[e.key]=false; });\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop() {\n  ctx.fillStyle="#0a0a2e"; ctx.fillRect(0,0,canvas.width,canvas.height);\n  if (keys["ArrowUp"]) py-=4; if(keys["ArrowDown"]) py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%90===0) enemies.push({x:600,y:Math.random()*350,w:35,h:35,speed:1+Math.random()*2});\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8; ctx.fillStyle="#00ff88"; ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){\n      if(collides(bullets[i],enemies[j])){\n        // ✏️ Incrémente le score de 100\n        enemies.splice(j,1); bullets.splice(i,1); break;\n      }\n    }\n  }\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed; ctx.fillStyle="#6b7280"; ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    if(enemies[i].x<-40) enemies.splice(i,1);\n  }\n  ctx.fillStyle="#fbbf24"; ctx.fillRect(px,py,30,30);\n  // ✏️ Affiche le score en haut à droite : ctx.fillText("Score: "+score, canvas.width-150, 25)\n  ctx.fillStyle="white"; ctx.font="16px Arial";\n  frame++; requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        solution: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px = 50, py = 180, score = 0;\nconst bullets = [], enemies = [];\nlet frame = 0;\nconst keys = {};\nwindow.addEventListener("keydown", (e) => { keys[e.key]=true; if(e.key===" ") bullets.push({x:px+30,y:py+12,w:12,h:4}); });\nwindow.addEventListener("keyup", (e) => { keys[e.key]=false; });\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop() {\n  ctx.fillStyle="#0a0a2e"; ctx.fillRect(0,0,canvas.width,canvas.height);\n  if (keys["ArrowUp"]) py-=4; if(keys["ArrowDown"]) py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%90===0) enemies.push({x:600,y:Math.random()*350,w:35,h:35,speed:1+Math.random()*2});\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8; ctx.fillStyle="#00ff88"; ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){\n      if(collides(bullets[i],enemies[j])){\n        score += 100;\n        enemies.splice(j,1); bullets.splice(i,1); break;\n      }\n    }\n  }\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed; ctx.fillStyle="#6b7280"; ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    if(enemies[i].x<-40) enemies.splice(i,1);\n  }\n  ctx.fillStyle="#fbbf24"; ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white"; ctx.font="16px Arial";\n  ctx.fillText("Score: "+score, canvas.width-150, 25);\n  frame++; requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        tests: [{ type: "match", expected: "score\\s*\\+=\\s*100" }, { type: "match", expected: "fillText.*Score" }],
        hint: "score += 100 quand un ennemi est touché, ctx.fillText pour afficher"
    },
    {
        id: "gd_c6_e2",
        title: "Game Over",
        category: "Jeu Complet",
        description: "Si le joueur n'a plus de PV — écran Game Over !",
        instruction: `Ajoute <code>playerHp = 3</code>. Si un ennemi passe le bord gauche, retire 1 PV. Si PV <= 0, arrête la boucle et affiche un écran "GAME OVER" avec le score final.`,
        code_template: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px=50,py=180,score=0,playerHp=3,gameOver=false;\nconst bullets=[],enemies=[];\nlet frame=0;\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{keys[e.key]=true;if(e.key===" "&&!gameOver)bullets.push({x:px+30,y:py+12,w:12,h:4});});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  // ✏️ Si gameOver, affiche l'écran Game Over et return (ne continue pas)\n  if(gameOver){\n    ctx.fillStyle="#0a0a2e"; ctx.fillRect(0,0,canvas.width,canvas.height);\n    ctx.fillStyle="#ff2244"; ctx.font="bold 48px Arial"; ctx.textAlign="center";\n    ctx.fillText("GAME OVER",canvas.width/2,180);\n    ctx.fillStyle="white"; ctx.font="24px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,230);\n    ctx.fillText("Appuie sur R pour rejouer",canvas.width/2,280);\n    ctx.textAlign="start";\n    return;\n  }\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%90===0)enemies.push({x:600,y:Math.random()*350,w:35,h:35,speed:1+Math.random()*2});\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){\n      if(collides(bullets[i],enemies[j])){score+=100;enemies.splice(j,1);bullets.splice(i,1);break;}\n    }\n  }\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed;ctx.fillStyle="#6b7280";ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    // ✏️ Si l'ennemi sort à gauche (x < -40), supprime-le ET retire 1 PV\n    // ✏️ Si playerHp <= 0, gameOver = true\n  }\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white";ctx.font="14px Arial";\n  ctx.fillText("Score: "+score+"  PV: "+playerHp,20,25);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        solution: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px=50,py=180,score=0,playerHp=3,gameOver=false;\nconst bullets=[],enemies=[];\nlet frame=0;\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{keys[e.key]=true;if(e.key===" "&&!gameOver)bullets.push({x:px+30,y:py+12,w:12,h:4});});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  if(gameOver){\n    ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n    ctx.fillStyle="#ff2244";ctx.font="bold 48px Arial";ctx.textAlign="center";\n    ctx.fillText("GAME OVER",canvas.width/2,180);\n    ctx.fillStyle="white";ctx.font="24px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,230);\n    ctx.fillText("Appuie sur R pour rejouer",canvas.width/2,280);\n    ctx.textAlign="start";return;\n  }\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%90===0)enemies.push({x:600,y:Math.random()*350,w:35,h:35,speed:1+Math.random()*2});\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){\n      if(collides(bullets[i],enemies[j])){score+=100;enemies.splice(j,1);bullets.splice(i,1);break;}\n    }\n  }\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed;ctx.fillStyle="#6b7280";ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    if(enemies[i].x<-40){enemies.splice(i,1);playerHp--;if(playerHp<=0)gameOver=true;}\n  }\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white";ctx.font="14px Arial";\n  ctx.fillText("Score: "+score+"  PV: "+playerHp,20,25);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        tests: [{ type: "match", expected: "playerHp--" }, { type: "match", expected: "gameOver\\s*=\\s*true" }],
        hint: "Quand un ennemi sort : playerHp--, si playerHp <= 0 : gameOver = true"
    },
    {
        id: "gd_c6_e3",
        title: "Restart du jeu",
        category: "Jeu Complet",
        description: "Appuie sur R pour recommencer — tout est réinitialisé !",
        instruction: `Écoute la touche "r" dans keydown. Si <code>gameOver</code> est true et que "r" est appuyé, réinitialise toutes les variables (score, PV, listes, etc.) et relance le jeu.`,
        code_template: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px=50,py=180,score=0,playerHp=3,gameOver=false,frame=0;\nconst bullets=[],enemies=[];\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{\n  keys[e.key]=true;\n  if(e.key===" "&&!gameOver) bullets.push({x:px+30,y:py+12,w:12,h:4});\n  // ✏️ Si e.key === "r" et gameOver → réinitialise tout et relance\n  // score=0; playerHp=3; gameOver=false; frame=0;\n  // bullets.length=0; enemies.length=0; py=180;\n  // requestAnimationFrame(gameLoop);\n});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  if(gameOver){\n    ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n    ctx.fillStyle="#ff2244";ctx.font="bold 48px Arial";ctx.textAlign="center";\n    ctx.fillText("GAME OVER",canvas.width/2,180);\n    ctx.fillStyle="white";ctx.font="20px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,230);\n    ctx.fillText("Appuie sur R",canvas.width/2,270);\n    ctx.textAlign="start";return;\n  }\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%80===0)enemies.push({x:600,y:Math.random()*350,w:35,h:35,speed:1.5+Math.random()*2});\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){if(collides(bullets[i],enemies[j])){score+=100;enemies.splice(j,1);bullets.splice(i,1);break;}}\n  }\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed;ctx.fillStyle="#6b7280";ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    if(enemies[i].x<-40){enemies.splice(i,1);playerHp--;if(playerHp<=0)gameOver=true;}\n  }\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white";ctx.font="14px Arial";\n  ctx.fillText("Score: "+score+"  PV: "+playerHp,20,25);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        solution: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px=50,py=180,score=0,playerHp=3,gameOver=false,frame=0;\nconst bullets=[],enemies=[];\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{\n  keys[e.key]=true;\n  if(e.key===" "&&!gameOver) bullets.push({x:px+30,y:py+12,w:12,h:4});\n  if(e.key==="r"&&gameOver){score=0;playerHp=3;gameOver=false;frame=0;bullets.length=0;enemies.length=0;py=180;requestAnimationFrame(gameLoop);}\n});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  if(gameOver){\n    ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n    ctx.fillStyle="#ff2244";ctx.font="bold 48px Arial";ctx.textAlign="center";\n    ctx.fillText("GAME OVER",canvas.width/2,180);\n    ctx.fillStyle="white";ctx.font="20px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,230);\n    ctx.fillText("Appuie sur R",canvas.width/2,270);\n    ctx.textAlign="start";return;\n  }\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%80===0)enemies.push({x:600,y:Math.random()*350,w:35,h:35,speed:1.5+Math.random()*2});\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){if(collides(bullets[i],enemies[j])){score+=100;enemies.splice(j,1);bullets.splice(i,1);break;}}\n  }\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed;ctx.fillStyle="#6b7280";ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    if(enemies[i].x<-40){enemies.splice(i,1);playerHp--;if(playerHp<=0)gameOver=true;}\n  }\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white";ctx.font="14px Arial";\n  ctx.fillText("Score: "+score+"  PV: "+playerHp,20,25);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        tests: [{ type: "match", expected: "e\\.key\\s*===\\s*\"r\"" }, { type: "match", expected: "bullets\\.length\\s*=\\s*0" }],
        hint: "Réinitialise TOUTES les variables : score, hp, gameOver, frame, bullets, enemies, position"
    },
    {
        id: "gd_c6_e4",
        title: "Boss final — XANA",
        category: "Jeu Complet",
        description: "Affronte le boss ultime : XANA avec 20 PV et tir en éventail !",
        instruction: `Crée un boss avec <code>hp: 20</code> qui tire en éventail (3 projectiles) toutes les 50 frames et qui se déplace verticalement. Quand le boss est vaincu, affiche "VICTOIRE !".`,
        code_template: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px=50,py=180,score=0;\nconst bullets=[],bossBullets=[];\nconst boss={x:480,y:150,w:70,h:70,hp:20,dir:1,speed:1.5};\nlet frame=0,victory=false;\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{keys[e.key]=true;if(e.key===" ")bullets.push({x:px+30,y:py+12,w:12,h:4});});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  if(victory){\n    ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n    ctx.fillStyle="#00ff88";ctx.font="bold 48px Arial";ctx.textAlign="center";\n    ctx.fillText("VICTOIRE !",canvas.width/2,180);\n    ctx.fillStyle="white";ctx.font="20px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,230);\n    ctx.textAlign="start";return;\n  }\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  // ✏️ Déplace le boss : boss.y += boss.speed * boss.dir\n  // ✏️ Inverse si boss.y > 320 ou boss.y < 20\n  // ✏️ Toutes les 50 frames, le boss tire 3 projectiles en éventail\n  if(frame%50===0){\n    for(const a of [-0.4,0,0.4]){\n      // ✏️ bossBullets.push({x:boss.x,y:boss.y+35,vx:Math.cos(Math.PI+a)*5,vy:Math.sin(Math.PI+a)*5})\n    }\n  }\n  // Joueur tire\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    if(collides(bullets[i],boss)){boss.hp--;score+=50;bullets.splice(i,1);if(boss.hp<=0)victory=true;}\n  }\n  // Boss bullets\n  for(let i=bossBullets.length-1;i>=0;i--){\n    const b=bossBullets[i];b.x+=b.vx;b.y+=b.vy;\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(b.x,b.y,4,0,Math.PI*2);ctx.fill();\n    if(b.x<-10||b.y<-10||b.y>410)bossBullets.splice(i,1);\n  }\n  // Boss\n  ctx.fillStyle=boss.hp>10?"#4b0082":"#8b0000";\n  ctx.fillRect(boss.x,boss.y,boss.w,boss.h);\n  ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(boss.x+35,boss.y+35,18,0,Math.PI*2);ctx.fill();\n  // Boss HP bar\n  ctx.fillStyle="#374151";ctx.fillRect(boss.x,boss.y-12,boss.w,8);\n  ctx.fillStyle="#ff2244";ctx.fillRect(boss.x,boss.y-12,(boss.hp/20)*boss.w,8);\n  // Player\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white";ctx.font="14px Arial";\n  ctx.fillText("Score: "+score+"  Boss HP: "+boss.hp,20,25);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        solution: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet px=50,py=180,score=0;\nconst bullets=[],bossBullets=[];\nconst boss={x:480,y:150,w:70,h:70,hp:20,dir:1,speed:1.5};\nlet frame=0,victory=false;\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{keys[e.key]=true;if(e.key===" ")bullets.push({x:px+30,y:py+12,w:12,h:4});});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction collides(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  if(victory){\n    ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n    ctx.fillStyle="#00ff88";ctx.font="bold 48px Arial";ctx.textAlign="center";\n    ctx.fillText("VICTOIRE !",canvas.width/2,180);\n    ctx.fillStyle="white";ctx.font="20px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,230);\n    ctx.textAlign="start";return;\n  }\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  boss.y+=boss.speed*boss.dir;\n  if(boss.y>320||boss.y<20)boss.dir*=-1;\n  if(frame%50===0){\n    for(const a of [-0.4,0,0.4]){\n      bossBullets.push({x:boss.x,y:boss.y+35,vx:Math.cos(Math.PI+a)*5,vy:Math.sin(Math.PI+a)*5});\n    }\n  }\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    if(collides(bullets[i],boss)){boss.hp--;score+=50;bullets.splice(i,1);if(boss.hp<=0)victory=true;}\n  }\n  for(let i=bossBullets.length-1;i>=0;i--){\n    const b=bossBullets[i];b.x+=b.vx;b.y+=b.vy;\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(b.x,b.y,4,0,Math.PI*2);ctx.fill();\n    if(b.x<-10||b.y<-10||b.y>410)bossBullets.splice(i,1);\n  }\n  ctx.fillStyle=boss.hp>10?"#4b0082":"#8b0000";\n  ctx.fillRect(boss.x,boss.y,boss.w,boss.h);\n  ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(boss.x+35,boss.y+35,18,0,Math.PI*2);ctx.fill();\n  ctx.fillStyle="#374151";ctx.fillRect(boss.x,boss.y-12,boss.w,8);\n  ctx.fillStyle="#ff2244";ctx.fillRect(boss.x,boss.y-12,(boss.hp/20)*boss.w,8);\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="white";ctx.font="14px Arial";\n  ctx.fillText("Score: "+score+"  Boss HP: "+boss.hp,20,25);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        tests: [{ type: "contains", expected: "bossBullets.push" }, { type: "match", expected: "boss\\.dir\\s*\\*=\\s*-1" }],
        hint: "Le boss se déplace verticalement et tire en éventail"
    },
    {
        id: "gd_c6_e5",
        title: "🎮 Jeu Complet !",
        category: "Jeu Complet",
        description: "Assemble tout — écran titre → combat → boss → victoire !",
        instruction: `C'est le projet final ! Le jeu a 3 phases : <strong>title</strong> (appuie Entrée), <strong>play</strong> (vagues d'ennemis + boss à la vague 5), <strong>end</strong> (victoire ou game over). Complète la logique pour spawn le boss à la vague 5 et déclencher la victoire.`,
        code_template: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet phase="title",px=50,py=180,score=0,hp=5,frame=0,wave=0;\nconst bullets=[],enemies=[],eBullets=[];\nlet boss=null;\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{\n  keys[e.key]=true;\n  if(e.key==="Enter"&&phase==="title"){phase="play";}\n  if(e.key===" "&&phase==="play")bullets.push({x:px+30,y:py+12,w:12,h:4});\n  if(e.key==="r"&&phase==="end"){phase="play";score=0;hp=5;frame=0;wave=0;boss=null;bullets.length=0;enemies.length=0;eBullets.length=0;py=180;}\n});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction col(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\n\nfunction gameLoop(){\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n\n  if(phase==="title"){\n    ctx.fillStyle="#00d4ff";ctx.font="bold 36px Arial";ctx.textAlign="center";\n    ctx.fillText("⚔️ LYOKO DEFENDER",canvas.width/2,150);\n    ctx.fillStyle="white";ctx.font="18px Arial";\n    ctx.fillText("Protège Lyoko contre les forces de XANA",canvas.width/2,200);\n    ctx.fillStyle="#fbbf24";ctx.font="16px Arial";\n    ctx.fillText("Appuie sur ENTRÉE pour commencer",canvas.width/2,280);\n    ctx.fillText("↑↓ = Déplacer  |  Espace = Tirer",canvas.width/2,310);\n    ctx.textAlign="start";requestAnimationFrame(gameLoop);return;\n  }\n  if(phase==="end"){\n    ctx.textAlign="center";\n    ctx.fillStyle=hp>0?"#00ff88":"#ff2244";ctx.font="bold 42px Arial";\n    ctx.fillText(hp>0?"VICTOIRE !":"GAME OVER",canvas.width/2,170);\n    ctx.fillStyle="white";ctx.font="20px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,220);\n    ctx.fillText("Appuie sur R",canvas.width/2,260);\n    ctx.textAlign="start";return;\n  }\n\n  // === PLAY ===\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n\n  // ✏️ Vagues : toutes les 150 frames, incrémente wave et spawn 3+(wave) ennemis\n  // ✏️ Si wave === 5 et pas de boss → boss = {x:520,y:150,w:60,h:60,hp:15,dir:1}\n  if(frame%150===0&&!boss){\n    // ✏️ wave++ et spawn ennemis\n    // ✏️ Si wave >= 5, crée le boss\n  }\n\n  // Boss mouvement + tir\n  if(boss&&boss.hp>0){\n    boss.y+=1.5*boss.dir;if(boss.y>320||boss.y<20)boss.dir*=-1;\n    if(frame%45===0)for(const a of[-0.4,0,0.4])eBullets.push({x:boss.x,y:boss.y+30,vx:Math.cos(Math.PI+a)*5,vy:Math.sin(Math.PI+a)*5,w:8,h:8});\n  }\n\n  // Player bullets\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){if(col(bullets[i],enemies[j])){score+=100;enemies.splice(j,1);bullets.splice(i,1);break;}}\n    if(boss&&boss.hp>0&&bullets[i]&&col(bullets[i],boss)){boss.hp--;score+=50;bullets.splice(i,1);}\n  }\n\n  // ✏️ Si boss existe et boss.hp <= 0 → phase = "end" (victoire)\n\n  // Enemies\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed;ctx.fillStyle="#6b7280";ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(enemies[i].x+17,enemies[i].y+17,6,0,Math.PI*2);ctx.fill();\n    if(enemies[i].x<-40){enemies.splice(i,1);hp--;if(hp<=0)phase="end";}\n  }\n  // Enemy bullets\n  for(let i=eBullets.length-1;i>=0;i--){\n    eBullets[i].x+=eBullets[i].vx;eBullets[i].y+=eBullets[i].vy;\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(eBullets[i].x,eBullets[i].y,4,0,Math.PI*2);ctx.fill();\n    if(eBullets[i].x<-10||eBullets[i].y<-10||eBullets[i].y>410){eBullets.splice(i,1);continue;}\n    if(col(eBullets[i],{x:px,y:py,w:30,h:30})){hp--;eBullets.splice(i,1);if(hp<=0)phase="end";}\n  }\n  // Draw boss\n  if(boss&&boss.hp>0){\n    ctx.fillStyle="#4b0082";ctx.fillRect(boss.x,boss.y,boss.w,boss.h);\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(boss.x+30,boss.y+30,15,0,Math.PI*2);ctx.fill();\n    ctx.fillStyle="#374151";ctx.fillRect(boss.x,boss.y-10,boss.w,6);\n    ctx.fillStyle="#ff2244";ctx.fillRect(boss.x,boss.y-10,(boss.hp/15)*boss.w,6);\n  }\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  // HUD\n  ctx.fillStyle="#374151";ctx.fillRect(20,12,100,10);ctx.fillStyle="#00ff88";ctx.fillRect(20,12,(hp/5)*100,10);\n  ctx.fillStyle="white";ctx.font="12px Arial";\n  ctx.fillText("Score: "+score+"  Vague: "+wave+(boss?" | BOSS":""),20,38);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        solution: `const canvas = document.getElementById("game");\nconst ctx = canvas.getContext("2d");\nlet phase="title",px=50,py=180,score=0,hp=5,frame=0,wave=0;\nconst bullets=[],enemies=[],eBullets=[];\nlet boss=null;\nconst keys={};\nwindow.addEventListener("keydown",(e)=>{\n  keys[e.key]=true;\n  if(e.key==="Enter"&&phase==="title"){phase="play";}\n  if(e.key===" "&&phase==="play")bullets.push({x:px+30,y:py+12,w:12,h:4});\n  if(e.key==="r"&&phase==="end"){phase="play";score=0;hp=5;frame=0;wave=0;boss=null;bullets.length=0;enemies.length=0;eBullets.length=0;py=180;}\n});\nwindow.addEventListener("keyup",(e)=>{keys[e.key]=false;});\nfunction col(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y;}\nfunction gameLoop(){\n  ctx.fillStyle="#0a0a2e";ctx.fillRect(0,0,canvas.width,canvas.height);\n  if(phase==="title"){\n    ctx.fillStyle="#00d4ff";ctx.font="bold 36px Arial";ctx.textAlign="center";\n    ctx.fillText("⚔️ LYOKO DEFENDER",canvas.width/2,150);\n    ctx.fillStyle="white";ctx.font="18px Arial";\n    ctx.fillText("Protège Lyoko contre les forces de XANA",canvas.width/2,200);\n    ctx.fillStyle="#fbbf24";ctx.font="16px Arial";\n    ctx.fillText("Appuie sur ENTRÉE pour commencer",canvas.width/2,280);\n    ctx.fillText("↑↓ = Déplacer  |  Espace = Tirer",canvas.width/2,310);\n    ctx.textAlign="start";requestAnimationFrame(gameLoop);return;\n  }\n  if(phase==="end"){\n    ctx.textAlign="center";\n    ctx.fillStyle=hp>0?"#00ff88":"#ff2244";ctx.font="bold 42px Arial";\n    ctx.fillText(hp>0?"VICTOIRE !":"GAME OVER",canvas.width/2,170);\n    ctx.fillStyle="white";ctx.font="20px Arial";\n    ctx.fillText("Score: "+score,canvas.width/2,220);\n    ctx.fillText("Appuie sur R",canvas.width/2,260);\n    ctx.textAlign="start";return;\n  }\n  if(keys["ArrowUp"])py-=4;if(keys["ArrowDown"])py+=4;\n  py=Math.max(0,Math.min(360,py));\n  if(frame%150===0&&!boss){\n    wave++;\n    for(let i=0;i<3+wave;i++)enemies.push({x:600+i*40,y:Math.random()*350,w:35,h:35,speed:1+Math.random()*2});\n    if(wave>=5)boss={x:520,y:150,w:60,h:60,hp:15,dir:1};\n  }\n  if(boss&&boss.hp>0){\n    boss.y+=1.5*boss.dir;if(boss.y>320||boss.y<20)boss.dir*=-1;\n    if(frame%45===0)for(const a of[-0.4,0,0.4])eBullets.push({x:boss.x,y:boss.y+30,vx:Math.cos(Math.PI+a)*5,vy:Math.sin(Math.PI+a)*5,w:8,h:8});\n  }\n  for(let i=bullets.length-1;i>=0;i--){\n    bullets[i].x+=8;ctx.fillStyle="#00ff88";ctx.fillRect(bullets[i].x,bullets[i].y,12,4);\n    if(bullets[i].x>600){bullets.splice(i,1);continue;}\n    for(let j=enemies.length-1;j>=0;j--){if(col(bullets[i],enemies[j])){score+=100;enemies.splice(j,1);bullets.splice(i,1);break;}}\n    if(boss&&boss.hp>0&&bullets[i]&&col(bullets[i],boss)){boss.hp--;score+=50;bullets.splice(i,1);}\n  }\n  if(boss&&boss.hp<=0)phase="end";\n  for(let i=enemies.length-1;i>=0;i--){\n    enemies[i].x-=enemies[i].speed;ctx.fillStyle="#6b7280";ctx.fillRect(enemies[i].x,enemies[i].y,35,35);\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(enemies[i].x+17,enemies[i].y+17,6,0,Math.PI*2);ctx.fill();\n    if(enemies[i].x<-40){enemies.splice(i,1);hp--;if(hp<=0)phase="end";}\n  }\n  for(let i=eBullets.length-1;i>=0;i--){\n    eBullets[i].x+=eBullets[i].vx;eBullets[i].y+=eBullets[i].vy;\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(eBullets[i].x,eBullets[i].y,4,0,Math.PI*2);ctx.fill();\n    if(eBullets[i].x<-10||eBullets[i].y<-10||eBullets[i].y>410){eBullets.splice(i,1);continue;}\n    if(col(eBullets[i],{x:px,y:py,w:30,h:30})){hp--;eBullets.splice(i,1);if(hp<=0)phase="end";}\n  }\n  if(boss&&boss.hp>0){\n    ctx.fillStyle="#4b0082";ctx.fillRect(boss.x,boss.y,boss.w,boss.h);\n    ctx.fillStyle="#ff2244";ctx.beginPath();ctx.arc(boss.x+30,boss.y+30,15,0,Math.PI*2);ctx.fill();\n    ctx.fillStyle="#374151";ctx.fillRect(boss.x,boss.y-10,boss.w,6);\n    ctx.fillStyle="#ff2244";ctx.fillRect(boss.x,boss.y-10,(boss.hp/15)*boss.w,6);\n  }\n  ctx.fillStyle="#fbbf24";ctx.fillRect(px,py,30,30);\n  ctx.fillStyle="#374151";ctx.fillRect(20,12,100,10);ctx.fillStyle="#00ff88";ctx.fillRect(20,12,(hp/5)*100,10);\n  ctx.fillStyle="white";ctx.font="12px Arial";\n  ctx.fillText("Score: "+score+"  Vague: "+wave+(boss?" | BOSS":""),20,38);\n  frame++;requestAnimationFrame(gameLoop);\n}\ngameLoop();`,
        tests: [{ type: "match", expected: "wave\\+\\+" }, { type: "match", expected: "wave\\s*>=\\s*5" }, { type: "match", expected: "phase\\s*=\\s*\"end\"" }],
        hint: "wave++ incrémente la vague. À la vague 5, crée le boss. Si boss.hp <= 0, victoire !"
    },
];

addExercises("gamedev", exercises);
